var animals = ["Dog", "Cat", "Rabbit"];
var messages = ["is already a good pet", "can be a good pet", "eats lot of grass"];
for(var i = 0; i < animals.length; i++){
    console.log(animals[i] + " " + messages[i]);
}
console.log("These all are can be a good pet");